import bytesockets.ByteClientSocket;

import java.io.IOException;

public class FileReaderMain {
  static public void main(String[] args) {
   /* try {
      FileMonitorManager fileMonitorManager = new FileMonitorManager();
      ByteClientSocket byteClientSocket = new ByteClientSocket("127.0.0.1", 6666, bytes->{});
      fileMonitorManager.startMonitoring("./monitored", byteClientSocket);
      System.in.read();
      fileMonitorManager.stopMonitoring();

    }
    catch (IOException e) {
      e.printStackTrace();
    }*/
  }
}
